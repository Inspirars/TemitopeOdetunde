// mobile

// 18px clamp(14px,4.205vw,18px)


// desktop font sizes

// 20px clamp(16px,1.38vw,20px)

// 24px = clamp(18px,1.666vw,24px)

// 32px = clamp(24px,2.22vw,32px)

// 40px clamp(30px,2.77vw,40px)

// 64px clamp(45px,4vw,64px)

// 94px = clamp(85px,6.52vw,94px)

